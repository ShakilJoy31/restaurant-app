import 'swiper/css';
import Reservation from '../components/Reservation';

const Reservations = () => {
    return (
        <div>
            <Reservation></Reservation>
        </div>
    );
};

export default Reservations;